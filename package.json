{
  "name": "afip-automation-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp manifest.json dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
